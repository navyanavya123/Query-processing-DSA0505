import pandas as pd

# Sample data dictionary with index labels
sample_data = {'X': [78, 85, 96, 80, 86], 'Y': [84, 94, 89, 83, 86], 'Z': [86, 97, 96, 72, 83]}
index_labels = ['A', 'B', 'C', 'D', 'E']

# Create DataFrame from dictionary with specified index labels
df = pd.DataFrame(sample_data, index=index_labels)

# Display the DataFrame
print("DataFrame from dictionary with index labels:")
print(df)

